---
title: "LeetCode 102 — Binary Tree Level Order Traversal"
date: 2025-10-25 22:07:16
slug: leetcode-102-BinaryTreeLevelOrderTraversal # optional, used for :title
tags: [Tree, Breadth-First Search, Binary Tree]
categories: [LeetCode]
hidden: true
comments: true
---

**Problem:** [Binary Tree Level Order Traversal](https://leetcode.com/problems/binary-tree-level-order-traversal/description/)  
**Difficulty:** Medium

---

## 🧠 Approach

We just need to use BFS and append the values at each level into the same `temp` array before appending the `temp` array into the `res` array.

---

## ⏱️ Complexity

Time Complexity: O(n)

Each node in the tree is visited exactly once. The operations within the loop (append, popleft) for the deque are O(1). For n nodes, we perform O(1) work for each node

Space Complexity: O(w)

Where w is the maximum width of the binary tree. The queue will store at most w nodes at any given time. In the worst case (complete binary tree), the maximum width could be n/2, making it O(n). The result list res stores all n values but this is considered output space

---

## ⏱️ Complexity

|          | Time   | Space  |
| -------- | ------ | ------ |
| Solution | _O(n)_ | _O(n)_ |

## ✅ Code (Python)

```python
from collections import deque

# Definition for a binary tree node.
# class TreeNode:
#     def __init__(self, val=0, left=None, right=None):
#         self.val = val
#         self.left = left
#         self.right = right
class Solution:
    def levelOrder(self, root: Optional[TreeNode]) -> List[List[int]]:
        if root == None:
            return []

        # Create a deque object
        q = deque()

        # Push the root node and its value into q
        q.append((root, root.val))

        # Use a while-loop to perform BFS
        res = []
        temp = []
        while q:
            n = len(q)
            temp.clear()
            for i in range(n):
                # Pop the node and the value out of the q
                node, value = q.popleft()
                temp.append(value)
                # Append the left and right node into the q
                if node.left:
                    q.append([node.left, node.left.val])
                if node.right:
                    q.append([node.right, node.right.val])
            res.append(temp.copy())
        # Return the res
        return res
```
